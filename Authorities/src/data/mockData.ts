/**
 * Mock data is no longer used — the authority portal now connects
 * to the real CiviLens backend API (same database as the mobile app).
 *
 * This file is kept as a placeholder for backward compatibility.
 */
export {};
