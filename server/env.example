# ──────────────────────────────────────────────────────────────────────────────
# CiviLens Server — Environment Variables
# Copy to .env and fill in your values.
# On Railway, set these in the Variables tab (no .env file needed).
# ──────────────────────────────────────────────────────────────────────────────

# ── Database (PostgreSQL) ────────────────────────────────────────────────────
# Railway: Use the DATABASE_URL from the Railway Postgres plugin
DATABASE_URL="postgresql://postgres:password@localhost:5432/civics"
DIRECT_URL="postgresql://postgres:password@localhost:5432/civics"

# ── JWT ──────────────────────────────────────────────────────────────────────
# IMPORTANT: Generate a strong random secret for production!
# openssl rand -base64 48
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="30d"

# ── Server ───────────────────────────────────────────────────────────────────
PORT=4000
NODE_ENV=development

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins.
# Empty or omitted = reflect any origin (development).
# In production, restrict to your deployed frontend URLs.
ALLOWED_ORIGINS="http://localhost:8080,http://localhost:3000,https://your-app.vercel.app"

# ── Cloudinary (optional — for image uploads) ────────────────────────────────
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# ── Twilio (OTP via Verify API) ──────────────────────────────────────────────
# Get these from https://console.twilio.com
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_VERIFY_SERVICE_SID=""

# Set to "true" to send real OTP via Twilio, "false" for mocked OTP (dev only)
USE_TWILIO_OTP=false
